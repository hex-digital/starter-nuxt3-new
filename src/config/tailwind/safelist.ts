// Use the safelist for ensuring classes made it to the final CSS file, even if they're not found in the codebase.
// Great for when classes are applied via a CMS or some other dynamic methods.
export const safelist = [];
